var session = function(numTargets){
	//generate the targets
	this.missions = [];
	for(var i = 0; i <= numTargets; i++){
		missions.push(targets.generateTarget());
	}

	//get the gifts
	var inPlay = new inplay(giftDeck.draw(), giftDeck.draw(), giftDeck.draw());

	
};