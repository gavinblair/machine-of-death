#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ngen/bin/ngen" "$@"
  ret=$?
else 
  node  "$basedir/../ngen/bin/ngen" "$@"
  ret=$?
fi
exit $ret
